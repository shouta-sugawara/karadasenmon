<tr>
  <td><%= blog.store_type %></td>
  <td><%= blog.title %></td>
  <td><%= strip_tags(blog.content).try(:first, 50) %></td>
  <td><%= blog.status %></td>
  <td><%= link_to '編集', edit_admin_blog_path(blog.id), class: 'btn btn-primary' %></td>
</tr>
